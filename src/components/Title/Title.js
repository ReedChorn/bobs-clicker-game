import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Bob's Burgers Clicker Game!</h1>
        <p>Click on a character to advance, but don't click the same character twice!</p>
    </div>
);

export default Title;

